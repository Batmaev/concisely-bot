# Бот-саммаризатор для Telegram

Технологии:
- aiogram
- open router
- surrealdb
- docker compose
- uv


Принцип работы:
1. Каждое сообщение сохраняется в БД
2. Раз в 1000 сообщений генерируется запрос к openrouter
3. Результат отправляется в тот же чат


Упрощения:
- чаты известны заранее, их ID будут прописаны в конфиге
- базу данных можно запускать в unauthenticated-режиме (или с фиксированным user=root pass=root), не будем открывать её во внешний интернет


Промпт выглядит следующим образом:
````
Ты — бот-саммаризатор сообщений в Telegram.

Сообщения поступают в формате:
```
### ID Name
  text
```
или
```
### ID Name [reply to ID]
  text
```

Перескажи самые интересные / смешные моменты. 

Требования:
0. Язык ответа — русский
1. Длина — приблизительно до 1200 символов
2. Пиши только сам пересказ! Без фразы "Вот основные моменты", без заголовка "Пересказ", без рассуждений о чате в целом.
3. Для форматирования используй html (не markdown).
4. Используй только теги, поддерживаемые Telegram:
   - <b>текст</b> (жирный)
   - <i>текст</i> (курсив)
   - <a href="URL">текст</a> (ссылки)
5. Вместо списков (<ul>) используй символы-буллеты (• или -) и обычный перенос строки (\\n).
6. Обязательно закрывай все теги.

<messages>
...
</messages>
````

Я протестировал и обнаружил, что несколько моделей работают достаточно хорошо:
```
models = [
    'anthropic/claude-sonnet-4.5',
    'anthropic/claude-opus-4.5',
    'google/gemini-2.5-pro',
    'google/gemini-3-pro-preview',
    'openai/gpt-5.2',
    'x-ai/grok-4.1-fast',
]
```

Модель `'anthropic/claude-opus-4.5'` работает лучше всего.

Модель будет выбираться случайно, с вероятностью около 4/9 нужно выбирать Opus 4.5, с вероятностями 1/9 — любую оставшуюся модель. Скорее всего, это удобно реализовать через random.choice.


Постобработка:

Ответ модели можно жестко обрезать на 3000 символов

Затем попытаться пофиксить html-теги, скорее всего, подойдет библиотека Beautiful Soup

Затем добавить в начало хештэг `#concisely\n\n`, в конец — надпись `\n\nМодель: claude-opus-4.5` (с той моделью, которая действительно выбрана)

Если сообщение отправить не удалось из-за ошибки в html, следует отправить его обычным текстом.


Ловушки:

Скорее всего, нужно хранить в БД ID последнего саммаризованного сообщения. Если ID нового сообщения отличается на >= 1000, то сделать запрос к модели. Запросы долгие, за это время может успеть прийти еще одно сообщение. Чтобы это не вызвало повторную генерацию, надо хранить в памяти lock-переменную. Если создание и отправка саммари завершились без ошибки, lock-переменная освобождается, счетчик сдвигается на 1000.

В объектах сообщений, которые присылает aiogram, не всегда есть поле текст.
Иногда текст находится в поле caption (если пришло фото).
Если пришел стикер, в качестве текста можно написать что-то вроде `  Sticker [😀]` (только указать реальный эмодзи, соответствующий стикеру)

Текст сообщений может содержать переводы строки, символы '##' и тд. Чтобы модель не путалась, мы обязательно делаем отступ в начале каждой строчки из двух пробелов. Например:
```
### 120455 Uliana Dmitrieva [reply to 120352]
  я короче жду когда потеплеет
  мб в марте 
  и попробую взять велик в аренду
```
